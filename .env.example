# Cowork Sandbox Environment Configuration
# Copy to .env and fill in actual values

# Claude Code API (required for third-party providers)
ANTHROPIC_AUTH_TOKEN=your-api-token-here
ANTHROPIC_BASE_URL=https://your-api-endpoint

# VM Configuration (optional)
COWORK_VM_NAME=sandbox
COWORK_WORKSPACE=/workspace

# Proxy Configuration (optional)
# COWORK_PROXY_HOST=192.168.5.2
# COWORK_PROXY_PORT=7890

# Custom Mount (optional, format: host_path:vm_path)
# Default workspace is ~/Downloads/cowork-workspace -> /workspace
# COWORK_MOUNT=~/myproject:/project

# Usage:
# 1. cp .env.example .env
# 2. Edit .env with actual values
# 3. source .env
# 4. ./scripts/cowork init
