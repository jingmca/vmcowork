# Cowork Sandbox 环境变量配置
# 复制此文件为 .env 并填入实际值

# Claude Code API 配置（必填）
ANTHROPIC_AUTH_TOKEN=your-api-token-here
ANTHROPIC_BASE_URL=https://your-api-endpoint

# VM 配置（可选）
COWORK_VM_NAME=sandbox
COWORK_WORKSPACE=/workspace

# 使用方式:
# 1. 复制此文件: cp .env.example .env
# 2. 编辑 .env 文件，填入实际的 token 和 URL
# 3. 加载环境变量: source .env
# 4. 运行 cowork: ./scripts/cowork ask "your prompt"
